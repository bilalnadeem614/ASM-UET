<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Account</title>
    <script src="https://cdn.tailwindcss.com"></script>

    <style>
        /* Smooth Glass Effect */
        .glass {
            background: rgba(255, 255, 255, 0.25);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
        }
    </style>
</head>

<body class="min-h-screen flex items-center justify-center bg-gradient-to-br from-sky-500 via-teal-500 to-emerald-500 p-5">

    <div class="glass w-[900px] rounded-3xl shadow-2xl flex overflow-hidden border border-white/20">
        <!-- Left Section -->
        <div class="w-1/2 bg-gradient-to-br from-sky-600/80 to-teal-700/80 text-white flex flex-col justify-center items-center p-12 relative">
            <h2 class="text-4xl font-extrabold mb-4 drop-shadow">Join Us</h2>
            <p class="text-white/90 text-center text-lg leading-relaxed">
                Create your personal account and begin your journey with us.
            </p>

            <!-- Decorative Waves -->
            <div class="absolute inset-0 opacity-15">
                <svg class="w-full h-full" xmlns="http://www.w3.org/2000/svg">
                    <path d="M0,120 Q150,200 300,120 T600,120" stroke="white" fill="none" stroke-width="3" />
                    <path d="M0,220 Q150,300 300,220 T600,220" stroke="white" fill="none" stroke-width="3" />
                </svg>
            </div>
        </div>

        <!-- Right Section -->
        <div class="w-1/2 bg-white/90 backdrop-blur-sm flex flex-col justify-center px-12 py-14">
            <h2 class="text-3xl font-bold text-gray-800 mb-8">Create Account</h2>

            <form asp-action="Register_Page" asp-controller="Signup" method="POST" class="space-y-6">

                <!-- Role Selection -->
                <div>
                    <select name="Role" required
                            class="w-full border border-gray-300 rounded-xl px-4 py-3 shadow-sm bg-white text-gray-700
                       focus:outline-none focus:ring-2 focus:ring-sky-500">
                        <option value="" disabled selected>Select Role</option>
                        <option value="Student">Student</option>
                        <option value="Teacher">Teacher</option>
                    </select>
                </div>

                <!-- Full Name -->
                <div>
                    <input type="text" name="FullName" placeholder="Full Name" required
                           class="w-full border border-gray-300 rounded-xl px-4 py-3 shadow-sm bg-white
                          focus:outline-none focus:ring-2 focus:ring-sky-500">
                </div>

                <!-- Email -->
                <div>
                    <input type="email" name="Email" placeholder="Email" required
                           class="w-full border border-gray-300 rounded-xl px-4 py-3 shadow-sm bg-white
                          focus:outline-none focus:ring-2 focus:ring-sky-500">
                </div>

                <!-- Password -->
                <div>
                    <input type="password" name="Password" placeholder="Password" required
                           class="w-full border border-gray-300 rounded-xl px-4 py-3 shadow-sm bg-white
                          focus:outline-none focus:ring-2 focus:ring-sky-500">
                </div>

                <!-- Confirm Password -->
                <div>
                    <input type="password" name="ConfirmPassword" placeholder="Confirm Password" required
                           class="w-full border border-gray-300 rounded-xl px-4 py-3 shadow-sm bg-white
                          focus:outline-none focus:ring-2 focus:ring-sky-500">
                </div>

                <!-- Button -->
                <button type="submit"
                        class="w-full bg-gradient-to-r from-sky-500 to-teal-600 text-white py-3 rounded-xl
                       font-semibold shadow-md hover:scale-[1.02] hover:shadow-lg transition-all duration-200">
                    Create Account
                </button>

                <!-- Login Link -->
                <p class="text-center text-gray-600 text-sm mt-4">
                    Already have an account?
                    <a href="@Url.Action("LoginPage", "Login")"
                       class="text-sky-600 font-medium hover:underline">Sign In</a>
                </p>

            </form>
        </div>


    </div>

</body>
</html>
